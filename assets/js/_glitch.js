$(document).ready(function(){
    setTimeout(function() {
    $('body').css('background-image','none');
    $('#freakshow > img').hide();
    },51700);

    // if ($.browser.msie) {
    //     $('#freakshow > img').hide();
    //     $('#frdmn img:last-child').hide();
    // }
});
